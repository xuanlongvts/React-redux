import React from 'react';

let AddTodo = ({dispatch}) => {

    return(
        <div>
            <form>
                <input />
                <button type="submit">Add Todo</button>
            </form>
        </div>
    )
};

export default AddTodo;